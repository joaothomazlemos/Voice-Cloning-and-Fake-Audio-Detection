from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='machine learning system that detect if a spoken audio is synthetically generated or not.',
    author='João Thomaz Lemos',
    license='MIT',
)
